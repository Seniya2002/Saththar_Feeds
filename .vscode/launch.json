{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open welcome.php via localhost",
            "url": "http://localhost/saththar_feeds/welcome.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
